# 练习 11

在现有研究员、写手之外增加 **critic（审稿）** 节点：

1. supervisor 的路由增加 `critic`
2. 写手完成后再进 critic：用两三句指出不足或给出「通过」
3. 步数上限仍要保留，避免循环
4. 打印全部 messages，应能看到 `researcher` / `writer` / `critic` 的名字

对照 `solution.py`。
