# 练习 01

不要先看 `solution.py`。新建 `my_solution.py`（或直接改一份副本）完成：

1. 创建 **两个** Chat Model：`temperature=0` 和 `temperature=1.2`。
2. 用同一句提示词各 `invoke` 一次：「写一个关于猫咪的短句」。
3. 打印两次结果，观察随机性差异。
4. 再用 `stream` 输出一首四行小诗，标题自定。

完成后对照 `solution.py`。
