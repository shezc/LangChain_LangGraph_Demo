# 练习 08

1. 增加 `search(query: str) -> str` 模拟搜索工具，固定返回一两句与「LangGraph checkpointer」相关的说明。
2. 把天气和搜索都绑到模型上，放进同一个 `ToolNode`。
3. 提问：「LangGraph 的 checkpointer 是做什么的？另外北京天气呢？」
4. 确认 messages 里出现了两次（或以上）工具调用。

对照 `solution.py`。
