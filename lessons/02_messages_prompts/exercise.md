# 练习 02

1. 用 `ChatPromptTemplate` 做一个翻译器：变量为 `source_lang`、`target_lang`、`text`。
2. 把「LangGraph 用图来编排 Agent」从中文译成英文。
3. 再构造一轮多轮对话：先问「用一个词概括 LangGraph」，再追问「为什么是这个词？」，历史里要带上模型的第一轮回复。

对照 `solution.py`。
